name: aws
runtime: nodejs16.x
region: ${opt:region,'us-east-1'}
stage: ${opt:stage,'dev'}
deploymentBucket: binance-assets-buyer
memorySize: 128
environment:
  API_KEY: ${env:API_KEY}
  API_SECRET: ${env:API_SECRET}
